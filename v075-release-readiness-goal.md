# v0.7.5 release readiness goal

Status: complete on 2026-09-19. Branch: `codex/v0.7.5-memory`. Changes remain uncommitted and unpublished.

## Objective

Finish the current release with an understandable Memory readiness summary, one complete final-tree verification run, an integrated recovery workflow, and concise user documentation. Preserve existing work, saved stories, frozen request evidence, and explicit author acceptance.

## Scope and acceptance

- [x] Show the selected writer's local recall, prewriting search, semantic configuration, relationship-link dependencies, and automatic link preparation status in Writing tools.
- [x] Resolve writer overrides and Story defaults using production rules. Identify the separate writer used for automatic preparation. Expose blocked, disabled, unavailable, and configuration-only states truthfully; no confidence score or inference requests.
- [x] Offer direct controls to open relevant memory preferences, model profiles, and relationship preparation. Handle loading/failure and character-lens scope without implying unsupported recall.
- [x] Verify the rendered summary and corrective navigation at desktop and mobile sizes, including an inactive dependency and a changed writer.
- [x] Pass the entire backend suite, frontend model checks, Ruff, ESLint, TypeScript, production build, and whitespace checks on the final code tree.
- [x] Use isolated data to restore an older archive, write, revise continuity, keep, fork, restart, export, and restore again. Verify preserved originals, accepted path separation, revision receipts, and no automatic replay. Record controlled-provider limitations.
- [x] Consolidate the quick setup path, optional model-call costs, current archive behavior, and v0.7.5 release notes; retain detailed historical evaluation evidence in its existing documents.

## Boundaries

No v0.8 features, automatic continuity checker/repairer, changes to retrieval algorithms or author acceptance, personal data edits, live-model quality claims, commit, push, tag, or publication. A readiness label describes saved configuration and current local status; only a draft receipt describes actual evidence supplied.

## Verification record

Memory readiness implementation and nine focused tests are complete. Desktop and mobile browser flows passed using an isolated controlled provider; no model calls were made by readiness.

The integrated restore exposed a release blocker: this feature branch starts at v0.6.1, while the published v0.7.0 tag contains manuscript/role/model changes not yet incorporated. Both lineages used archive versions 32/33 for different record shapes. An actual archive generated with tag v0.7.0 (format 33) was rejected. The first full check run was stopped before completion; it is not a passing final-tree result.

- [x] Incorporate the published v0.7.0 behavior while preserving this branch's memory/cleanup work, with a local pre-integration snapshot. Preserve both historical archive lineages with explicit validation and a new unambiguous current version.
- [x] Add a regression for genuine released archive shape, including manuscript content, and rerun the integrated workflow after recovery is corrected.

Earlier audit results remain historical until superseded by measured results here. No Git history or index changes are part of this integration.

### Compatibility correction

Integrated the 201 paths changed between the branch base and published commit `d1c08400c0b133d63a7fde56d44bdfd43f853151` into the existing working files, preserving the memory feature work. The pre-integration local versions, release versions, base versions, and merge manifest are retained in the artifact directory below. This incorporates released manuscripts, agent templates, mode/agency guidance, model controls, usage reporting, and post-acceptance assessment.

Archive format **37** accepts the distinct historical layouts only when both table groups and prompt heads match a supported complete shape. It preserves manuscripts and bookmarks alongside memory/cleanup records. A genuine format-33 fixture generated by the v0.7.0 code is retained in `tests/fixtures/v070-manuscript-archive.json.gz`, with provenance alongside it. Raw archive SHA-256: `7c8c54e66ae1844524fba04e06539661a09a849f7da4d0915db803b6821be5d0`.

New recall protocol **6** accounts for frozen mode/agency guidance and the saved context safety margin. Continuity revision protocol **2** includes those instructions exactly once and refuses insufficient capacity without dropping sources. Historical recall v1-v5 and revision v1 retain their original replay behavior. Model usage totals remain available on the final scheduling event.

### Recovery exercise

Used invented prose and a disposable SQLite database. Restored the genuine v0.7.0 archive with an existing manuscript and draft; ran ordinary prewriting recall and writing; requested a continuity revision; kept it; forked before the delivery outcome; restarted the whole application lifespan against the same database; exported both paths; then exported, validated and restored format 37.

Measured: **3 controlled provider calls** (recall, writer, revision), **4 main-path passages**, **2 fork passages**, **1 restart**, and **no additional calls on restart or restore**. The original draft stayed available, acceptance remained explicit, fork history stayed separate, and the revision's exact prompt/content survived restore with remapped source identity. This validates lifecycle and storage behavior, not model reasoning quality.

### Browser QA

Used bundled headless Chromium through Playwright because a Browser-plugin session was unavailable. Served the production build at `http://127.0.0.1:18771` with isolated data and a controlled provider; desktop **1440 x 1000**, mobile **390 x 844**, reduced motion.

- Readiness: **14 named checks**, including page identity, actual preference/profile saves, inactive dependency, missing embedding setup, writer override, separate preparation writer, relationship navigation, mobile mode changes, and Escape focus return. No model requests came from readiness.
- Revision: opened a concern, created an alternative, compared the original, and repeated at mobile size. Three candidates (original plus two revisions) remained unaccepted; the accepted seed passage stayed unchanged.
- Released manuscript: restored the actual old archive, checked its title and bookmark, read its scene at both viewport sizes, and returned to Memory readiness. No model requests were made.
- All captures had **zero horizontal overflow**, with **zero browser errors or console warnings**. Screenshots were visually inspected for readiness and mobile manuscript reading.
- Separate failure injection: checked loading, a controlled HTTP 503 readiness failure with no stale status rows, and successful explicit Refresh recovery. Expected failed GETs were confined to this exercise. The disposable QA server was stopped afterward and its port was verified closed.

Artifacts: `C:/Users/Jim/.codex/visualizations/2026/09/19/01a0b7b5-8808-74e3-b54e-6b62ea00d044/`.

| Evidence | File in artifact directory |
| --- | --- |
| Pre-integration preservation | `v075-integration/manifest.json` and sibling source snapshots |
| Lifecycle report and reproduction | `v075-recovery-result.json`, `v075-recovery.py` |
| Readiness report | `v075-readiness-qa.json` |
| Readiness failure recovery | `v075-readiness-error-qa.json` |
| Readiness captures | `v075-readiness-desktop-configured.png`, `v075-readiness-mobile.png` |
| Revision report | `v075-integrated-revision-qa.json` |
| Manuscript report | `v075-integrated-book-qa.json` |
| Manuscript captures | `v075-integrated-book-reader-desktop.png`, `v075-integrated-book-reader-mobile.png` |
| Complete passing check | `v075-final-checks-03.log` |
| Verified code/test tree | `v075-final-source-manifest.json`, `v075-source-manifest.py` |

### Final checks and limits

The final `check.ps1` run completed with exit **0** (`v075-final-checks-03.log`):

| Check | Result |
| --- | --- |
| Complete backend suite | **1,374 passed**, 2 dependency deprecation warnings, 901.46 seconds |
| Frontend model tests | **94 passed**, 0 failed |
| Ruff and ESLint | Passed |
| TypeScript and Vite production build | Passed |
| Git whitespace check | Passed |
| Code/test manifest before and after full verification | **887 files unchanged** |

The manifest SHA-256 is `7ce7bd0c0b161f86e9d101224a2c31df067c412d575e9aa68e95eb362e28a0ee`. Only documentation was finalized afterward. The two warnings concern Starlette's HTTPX test-client integration and AnyIO's deprecated BlockingPortal alias.

The first completed full run (`v075-final-checks-02.log`) had **1,371 passes and 3 failures** in 971.27 seconds. It uncovered duplicate legacy-fixture table removal, a historical recall fixture using newly composed prompts, and the final usage-event mismatch. All three were corrected and passed both focused regressions and the subsequent complete run. The interrupted first attempt and failed intermediate run are retained as evidence, not counted as passing release checks.

No live inference was needed for this readiness work. Historical live evaluations in the narrative audit predate this release integration; they are not new measurements of the composed v0.7.0 prompts. Dependable novel-length continuity, current live-provider quality, and dedicated ebook-reader rendering are not established by these controlled checks. Automatic continuity checking remains experimental. Nothing has been committed or published.
